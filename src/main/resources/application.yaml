server:
  port: 8080
  servlet:
    context-path: /api
  error:
    include-message: never

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/apsas_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: apsas
    password: apsas
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB

  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      auto-offset-reset: earliest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.json.use.type.headers: true
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "com.project.apsas.*"
      group-id: group_one
    producer:
      acks: all
      retries: 3
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.use.type.headers: true
    listener:
      ack-mode: manual_immediate
      concurrency: 3

cloudinary:
  cloud-name: ddcyjcpx0
  api-key: 197971381659871
  api-secret: 6g3wTrSYbG9B5T5kPps5olGXOuM
  option:
    folder-name: uploads/general
    resource-type: auto

jwt:
  signerKey: "your-256-bit-secret-your-256-bit-secret"

brevo:
  base-url: https://api.brevo.com/v3
  api-key: ${API_KEY_BREVO}
  template-id: 3
  sender:
    mail: nguyentrongduc447@gmail.com
    name: CodeVerse

mvc:
  throw-exception-if-no-handler-found: true

web:
  resources:
    add-mappings: false
