server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123
    url: jdbc:mysql://localhost:3306/apsas_db?serverTimezone=UTC&useSSL=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate